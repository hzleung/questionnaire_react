import router from 'umi/router';
import { Toast } from 'antd-mobile';
import { getScholarShip } from '@/services/api';


export default {
    namespace: 'questionnaire_intro',
    state: {
        mock:[],
    },
    effects: {
    },
    reducers: {
    },
};
